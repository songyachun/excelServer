# 版本日志

## 2026-05-02

### 新增功能
- **主题切换**：页面右上角新增主题选择器，支持「暖阳」「皓月」「深空」三套主题，选择后自动保存
- **多列表单**：新增对话框自动根据字段数量排成 2~3 列，小屏适配

### 优化
- **UI 全面升级**：玻璃态卡片设计、渐变按钮、柔和光影、入场动画
- **代码拆分**：将内联 CSS/JS 拆分为独立的 `style.css` 和 `app.js`，结构更清晰
- **页面布局**：通栏全宽设计，内容区域更紧凑
- **主题系统重构**：全部颜色使用 CSS 变量管理，切换主题即时过渡

### 其他
- VS Code 启动配置端口改为 8989

## 2026-05-01

### 新增功能
- **追加导入**：导入 Excel 时支持追加模式，新数据追加到现有数据尾部而非覆盖
- **操作日志**：记录导入、新增、删除、清空等操作，可在页面右上角「操作日志」按钮查看
- **数据自动备份**：每次保存或清空数据前，自动备份旧数据到 `data/backup/` 目录

### 优化
- 导入交互优化：区分「导入」（覆盖）和「追加导入」两种模式
- 数据持久化增强：服务端保存的同时写入 localStorage 作为备份
- 服务端 DELETE 接口清空前也自动备份数据

### 其他
- 按钮文案调整，操作流程更清晰
